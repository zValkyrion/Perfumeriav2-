# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# radar (app de campo, ver RADAR_PLAN.md)
radar/node_modules/
radar/.next/
radar/out/
radar/*.tsbuildinfo
radar/next-env.d.ts
radar/.sst/
# sst-env.d.ts SÍ se versiona: lo genera SST, pero el build del sitio corre a
# mitad del despliegue y en un checkout limpio todavía no existe. Sin él, la CI
# falla con "Property Elrey_proveedores does not exist on type Resource".
